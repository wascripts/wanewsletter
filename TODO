TODO de la version 2.3 de Wanewsletter

------------------

- S’assurer du bon fonctionnement sur la future branche PHP 5.1.x

- revoir la partie newsletter.php/class.form.php pour permettre les inscriptions à plusieurs listes en même temps
  ainsi que fournir une API utilisable dans d'autres applis PHP (phpBB, SPIP, ...)
- Rendre plus simple l'ajout et remplissage de champs supplémentaires
- Confirmation à chaque demande d'inscription, pas seulement la première <- option à régler dans les paramètres de
  la liste
- Tenir compte du fait que magic_quotes peut être à on ou off, et que les données dans $_POST, $_GET ...
  peuvent avoir été modifiées par l'application qui fait appel à l'API wanewsletter (ex: phpBB) donc
  un paramètre magic_quotes_gpc interne et null par défaut mais peut être positionné à true ou false

- Terminer support des chargements de newsletters externes
  - Pour le chargement via une URL, trouver moyen pour spécifier les URLs une fois seulement (cas de SPIP), puis 
    seulement avoir à indiquer de charger via les URLs habituelles

- Nouveaux formats possibles pour l'export d'emails ?
- Fournir un gestionnaire de bounces basique dans le répertoire contrib/ (en cours)
- Voir pour rendre refonctionnel la détection approfondie des emails (sinon, la supprimer, pas seulement la désactiver)

- Voir avec J.J Solari pour fct des javascript dans Safari
- Vérifier que les schémas de tables sont valides pour chaque db
- Réorganiser les scripts d’installation et mise à jour
  - suppression champs hébergeur (et version ?) de wa_config
  - ajout champ liste_public à wa_liste (avec DEFAULT '1') et dans les schémas

- vérifier bon fonctionnement de profil_cp.php, checker le code


Modifications sur la doc
------------------------

- Revoir la doc de manière générale
- compléter fichier CREDITS


Fait mais à peaufiner
------------------------

- Annuler un envoi
- Prendre en compte les problèmes de charset lors du chargement via une URL
- Requètes de functions.validate.php (? bof, ça marche donc bon...)


