TODO de la version 2.3 de Wanewsletter

------------------

- Compatibilité assurée avec PHP5
- Support de SQLite
- Ajouter support de MySQL via l’extension mysqli (facile à faire)

- Nouveaux formats possibles pour l'export d'emails
- Enregistrement de l'IP lors d'une inscription ? (et checker pour empécher le spam de DB)
- revoir la partie newsletter.php/class.form.php pour permettre les inscriptions à plusieurs listes en même temps
  ainsi que fournir une API utilisable dans d'autres applis PHP (phpBB, SPIP, ...)
- Fournir un gestionnaire de bounces basique dans le répertoire contrib/
- Pas de mention du code de compte si panneau de gestion de compte est désactivé (?)
- email envoyé à admin lors de nouvel inscription: Limiter aux inscriptions confirmées si liste demande confirmation ?
- confirmation à chaque demande d'inscription, pas seulement la première
- Permettre l'envoi d'emails multipart (text/html) ?
- Mettre à jour le module de statistiques (notamment le graphique à barre, voir /home/web/devel-zone/PHP/GD/barres.php)
- Voir pour rendre refonctionnel la détection approfondie des emails (sinon, la supprimer, pas seulement la désactiver)
- Petit module pour gérer la config, plutôt que de balader une variable $nl_config un peu partout, ce qui est assez crade
- Annuler un envoi
- Permettre l’utilisation de tags dans le sujet de la newsletter
- Charger un modèle de newsletter via une url ? (ce qu'a fait thierry gagnon, voir spip-contrib)
- Ajouter une partie dans faq.fr.html pour SQLite ?


Coding
------------------

- Trouver une solution pour ne plus avoir besoin de $waroot
- Vérifier réglages de mode lorsqu'on ouvre des fichiers
- Ne pas stocker le numéro de version du script dans la DB, c’est stupide.
- stats.php, ligne 125, si taille fichier = 0, warning php
- remplacer mysql_get_client_info() dans tools.php, ligne 239

Passer de SQLite 2.x à SQLite 3.x :
	$ sqlite-old old.db .dump | sqlite-new new.db


